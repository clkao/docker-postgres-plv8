# postgres-plv8

Docker image for running plv8 v1.4.4 on Postgres 9.5.

Based on the [official Postgres image](http://registry.hub.docker.com/_/postgres/).

## Usage

Start a postgres server container with persistent storage and give it a name:

```bash
$ docker run -d -v /tmp/data:/var/lib/postgresql/data --name some-postgres clkao/postgres-plv8:9.5-1.4
```

Start client container that links to the server container above as the "postgres" alias:

```
$ docker run --link some-postgres:postgres --rm -ti clkao/postgres-plv8:9.5-1.4 bash -c 'psql -U postgres -h $POSTGRES_PORT_5432_TCP_ADDR'
```
